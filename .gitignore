__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.env
config/config.toml
config/config.local.toml
/data/
/logs/
/reports/
/backtests/
# Pre-wipe archives from scripts/reset_paper_db.py. The .db files are already
# covered below, but the .sha256 sidecars are not -- and anchoring keeps this
# root-only (see the CLAUDE.md /data/ gotcha).
/backups/
*.db

# Claude Code local tooling/settings (not project source)
.claude/
graphify-out/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Deploy runtime (not committed)
deploy/config/
deploy/logs/
deploy/reports/
deploy/.env

# Ruflo local secrets and runtime data
.env.local
.env.*.local
.claude-flow/
.swarm/
.agents/
